// node.js的模块说白了就是一个js文件（相当于c++的库）,里面封装了一些功能和变量。
// 而在node.js以外，跟据js文件的功能不同，它也被叫做  插件，框架

// node.js模块分为3种：
// 1. node.js内置模块(node.js官方提供的，比如fs，path，http)
// 2. 用户自定义模块（用户创建的每个js文件都是一个自定义模块）
// 3. 第三方模块（使用前需要下载）